# Root History

This folder contains historical files moved out of the workspace root during cleanup.

## Archived Files

- `casey_status_partial.patch`

## Notes

- `casey_status_partial.patch` preserves a partial Salesforce controller refactor that was left as a standalone patch artifact in the workspace root.
- The patch appears historical and unreferenced; keep it for reference, not as an active delivery mechanism.