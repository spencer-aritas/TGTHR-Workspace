// Query Named Credential configuration
// Note: NamedCredential object has limited queryable fields for security

SELECT DeveloperName, MasterLabel 
FROM NamedCredential 
WHERE DeveloperName = 'Doc_Gen_Secure'
